class ProductModel {
  final String title, price, model, image;

  const ProductModel(
      {required this.title,
      required this.price,
      required this.model,
      required this.image});
}
